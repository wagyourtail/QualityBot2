.topbar
    a(href="/" id="title")
            b Quality Bot
    #guildLength= `Serving ${guildLength} guilds.`
    a(id="login" href=loginStatus ? "/dashboard" : "/login")
        if loginStatus
            img(src=`${userAvatar}` width='50px', height='50px')
            =userName
        else
            ="login"
